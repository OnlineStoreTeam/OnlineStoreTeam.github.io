to do
  admin-product
    form
      image
        домалювати по макету
        валідувати при onChange, а не onBlur
      save
        close form or no?
    table
      icon-more
        edit
    search
      колір хрестика
    кнопки фільтрації категорії
      додати обробники
      додати дінамічни стилі
    0 Product Displayed
      змінна для кількості товарів
      поточнити фразу
    pagination
  admin-product-item
    change icons


later
  видалити зайві шрифти
  admin
    redirect to adminProduct
    button Search - onClick
    button Admin - onClick
    button LogOut - onClick


plan
  routing
    захищений routing for admin & cabinet

-------------------- git --------------------

git clone https://github.com/OnlineStoreTeam/OnlineStoreTeam.github.io.git
git clone https://github.com/OnlineStoreTeam/online-store-back.git

git add .
git commit -m "commit"
git push

git switch -c admin
git push --set-upstream origin admin
git fetch
git pull

-------------------- npm --------------------

npm start
npm run build
npm run deploy

npx create-react-app .
npm install -D tailwindcss
  npx tailwindcss init
  content: [
    "./src/**/*.{js,jsx,ts,tsx}",
  ],
  @tailwind base;
  @tailwind components;
  @tailwind utilities;

npm i react-router-dom
npm install react-icons --save
npm i react-toastify
npm i react-hook-form
npm install react-paginate --save
npm install gh-pages --save-dev
  @reduxjs/toolkit
  react-redux
